services:
    db:
        image: postgres:17.5-alpine
        restart: always
        shm_size: 128mb
        env_file: .env
        ports:
            - 5432:5432

    adminer:
        image: adminer
        restart: always
        ports:
            - 8080:8080
